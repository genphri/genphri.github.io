# Task 14/50 of task_suite/tasks_llm.json -- lotion_application / back (sitting).
# Written by task_suite/room_suite.py configs; description [llm].
# Original: The person is sitting in a shallow accent chair, feet flat on the floor. The robot applies steady, firm pressure with its bare tip to rub barrier cream into the mid and upper back.
run_name: room2_t14_lotion_appli_back
task: bathing
robot: stretch
human_indices:
- 0
cuda_id: 0
task_description: An adult of average build sits in a shallow accent chair (seat depth about 40-45 cm) with feet flat on the floor and thighs separated, their mid and upper back (spine1 and spine2) clear above the backrest, and hands resting loosely on their knees. Starting from a clearly separated non-contact pose, the robot approaches from behind and comes down from above over the top edge of the chair back, keeping its base and arm entirely outside the chair back, wings, and armrests at all times. The robot presses a sponge pad held flat against the skin with steady, firm pressure to rub barrier cream across spine1 and spine2 in three smooth vertical passes, ensuring only the sponge pad makes contact while the rest of the robot stays completely clear. It then lifts the sponge pad cleanly off the body and retreats to a separated non-contact pose so the end of the task is unambiguous.
human_posture: sitting
furniture_name: bedroom_accent_chair_0
target_segments:
- spine1
- spine2
human_placement_impl: vlm
scene:
  enabled: true
  order: room_first
  room_policy: download_delete
  scene_tries: 16
  fetch_min_free_gb: 5
  exclude_support:
  - 23:bedroom_bed_0
vlm_model: gemini-3.7-flash
orchestrator:
  model: gemini-3.7-flash
  stage_attempt_cap: 5
  item_execution_cap: 40
  outer_loop_cap: 1
  llm_call_cap: 600
  review_on_success: true
  cross_validate_humans: 0
  stage_timeouts_s:
    per_vlm_iter: 1800.0
    per_cross_val_body: 300.0
    default: 3600.0
seated_view: back
