# Task 45/50 of task_suite/tasks_llm.json -- skin_assessment / right_elbow (lying).
# Written by task_suite/room_suite.py configs; description [llm].
# Original: The person is lying in bed just after waking up in the morning. The robot performs a quick spot check by pressing its tip against the outer edge of the right elbow, checking the blanching response to ensure circulation wasn't compromised during the night.
run_name: room2_t45_skin_assessm_right_elbow
task: scratching
robot: stretch
human_indices:
- 0
cuda_id: 0
task_description: An adult of average build is lying supine on a standard bed with a flat mattress. Both legs extend straight with slight separation, the left arm rests alongside the torso, and the right arm rests on the mattress beside the torso with the right elbow slightly angled outward to fully expose its lateral surface. Starting from a clearly separated non-contact pose, a robot approaches the right side of the bed carrying a scratcher tool. The robot performs a single blanching spot check by pressing the narrow tip lightly against the skin at the outer edge of the right elbow across the junction of the rightArm and rightForeArm, holding steady for one brief press before releasing. Only the narrow tip of the scratcher touches the skin, while the rest of the robot remains completely clear of the human and bed. The robot then lifts the tool vertically clear of the body and retreats to a clearly separated non-contact pose so the end of the task is unambiguous.
human_posture: lying
furniture_name: bed
target_segments:
- rightArm
- rightForeArm
human_placement_impl: vlm
scene:
  enabled: true
  order: room_first
  room_policy: download_delete
  scene_tries: 8
  fetch_min_free_gb: 5
  exclude_support:
  - 23:bedroom_bed_0
vlm_model: gemini-3.7-flash
orchestrator:
  model: gemini-3.7-flash
  stage_attempt_cap: 5
  item_execution_cap: 40
  outer_loop_cap: 1
  llm_call_cap: 600
  review_on_success: true
  cross_validate_humans: 0
  stage_timeouts_s:
    per_vlm_iter: 1800.0
    per_cross_val_body: 300.0
    default: 3600.0
